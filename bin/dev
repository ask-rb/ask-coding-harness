#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

mkdir -p data

echo "🚀 Askoda server — http://localhost:${PORT:-8080}"
bundle exec rackup config.ru -p ${PORT:-8080} -o ${HOST:-0.0.0.0}
